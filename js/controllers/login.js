/**
 * @author Bisike N. Nnadi
 */

var voltaic = angular.module("voltaicLogin", ["firebaseAuth"]);

voltaic.controller("MyAuthController", ["$scope", "$firebaseAuth",
  function($scope, $firebaseAuth) {}
]);